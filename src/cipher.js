window.cipher = {
  encode: (cart,cifrar) => {
    /* Acá va tu código */
    let msj ="";
    for(let i = 0; i< cifrar.lenght; i++ );
    
  },
  decode: (cartDos,descifrar) => {
    /* Acá va tu código */
    let msjdos="";
    for(let i = 0; i< descifrar.lenght; i++);
  }
}